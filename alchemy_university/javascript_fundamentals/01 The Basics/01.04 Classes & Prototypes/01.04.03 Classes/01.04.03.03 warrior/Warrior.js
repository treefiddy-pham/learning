// extend the Hero class to Warrior
const Hero = require('./Hero');

// extends syntax to inherit methods of Hero to Warrior.
class Warrior extends Hero {
    //nothing new to add now
}

module.exports = Warrior;