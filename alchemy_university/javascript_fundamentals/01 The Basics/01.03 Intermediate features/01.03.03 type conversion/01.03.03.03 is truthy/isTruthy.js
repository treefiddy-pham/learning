// takes in value 'a' and returns if it is truthy
function isTruthy(a) {
    //parentheses used as shortcut for boolean evaluation instead of a full if statement
    return (a);
}

module.exports = isTruthy;