// take in two values and combine them as strings
function combineToString(a, b) {
    return String(a) + String(b);
    //also acceptable
    //return a.tostring() + b.tostring();
}

module.exports = combineToString;